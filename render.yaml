services:
  - type: web
    name: voice-shield-ai
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true

